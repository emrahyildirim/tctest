<%= form_with(model: tc, local: true) do |form| %>
  <% if tc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tc.errors.count, "error") %> prohibited this tc from being saved:</h2>

      <ul>
      <% tc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tc_id %>
    <%= form.text_field :tc_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :surname %>
    <%= form.text_field :surname %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
